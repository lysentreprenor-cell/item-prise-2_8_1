run = "npm install --legacy-peer-deps && npx expo start --web"
entrypoint = "App.tsx"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm install --legacy-peer-deps && npx expo start --web"]

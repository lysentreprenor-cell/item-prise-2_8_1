run = "git fetch origin && git reset --hard origin/claude/global-resell-assistant-3GOP0 && npm install --legacy-peer-deps && npx vite --port 3000 --host 0.0.0.0"
entrypoint = "client/src/main.tsx"
modules = ["nodejs-22"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["node", "dist/index.cjs"]
deploymentTarget = "autoscale"
build = ["npm", "run", "build"]

[[ports]]
localPort = 3000
externalPort = 80

[[ports]]
localPort = 5000
externalPort = 5000

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "git fetch origin && git reset --hard origin/claude/global-resell-assistant-3GOP0 && npm install --legacy-peer-deps && npx vite --port 3000 --host 0.0.0.0"
waitForPort = 3000

[userenv]

[userenv.production]
PORT = "3000"
